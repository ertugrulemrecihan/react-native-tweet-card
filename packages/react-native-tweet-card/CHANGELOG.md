# Changelog

## 1.0.6

- Fixed `Tweet` rendering when Twitter omits entity arrays such as `symbols`, `urls`, `hashtags`, or `user_mentions`.

## 1.0.3

- Removed `phosphor-react-native`; icons are now bundled as SVG components via `react-native-svg`.

## 1.0.2

- Marked `phosphor-react-native` and `react-native-svg` as required peer dependencies.
- Updated README installation instructions.

## 1.0.1

- Renamed the repository and npm package to `react-native-tweet-card`.

## 1.0.0

- Initial release with `TweetCard`, `Tweet`, `useTweet`, and `TweetSkeleton`.
