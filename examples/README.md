# Examples

## expo

Example Expo (Router) app that uses the `react-native-tweet` package.

```bash
cd expo
npm install
npx expo start
```

The package is linked from the repo root via `file:../..`. Run `npm run build` in the root first to use the latest build.
